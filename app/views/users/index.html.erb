<%= link_to 'New', new_user_path, class: 'btn btn-primary' if policy(@users).new? %>
<table class="my-3 table table-stripped">
  <thead>
    <tr>
      <th>name</th>
      <th>user group</th>
    </tr>
  </thead>
  <tbody>
    <%= render @users%>
  </tbody>
</table>