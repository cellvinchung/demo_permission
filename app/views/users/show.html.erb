<%= link_to 'edit', edit_user_path(user), class: 'btn btn-primary' if policy(user).edit?%>
<dl class="row my-3">
  <dt class="col-sm-3">Email</dt>
  <dd class="col-sm-9"><%= user.email %></dd>

  <dt class="col-sm-3">User Group</dt>
  <dd class="col-sm-9">
    <%= user.user_group.name %>
  </dd>
</dl>